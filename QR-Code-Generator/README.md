# 📱 Python QR Code Generator

Bu proje, Python ve `pyqrcode` kütüphanesini kullanarak kullanıcıdan alınan herhangi bir URL veya metni **vektörel (SVG)** formatında QR koda dönüştüren bir araçtır.

Veri transferi ve dijital araçların otomasyonu üzerine yaptığım çalışmalardan biridir.

## 🚀 Özellikler

- **Hızlı Dönüştürme:** URL'yi saniyeler içinde QR koda çevirir.
- **Vektörel Çıktı (.svg):** Kalite kaybı olmadan istenilen boyuta büyütülebilir çıktı verir.
- **Kullanıcı Dostu:** Terminal üzerinden basit giriş-çıkış mantığı ile çalışır.

## 🛠️ Kullanılan Teknolojiler

- **Python 3.x**
- **[pyqrcode](https://pypi.org/project/PyQRCode/):** QR kod şifreleme algoritması için.

## 📦 Kurulum

1. Projeyi indirin.
2. Gerekli kütüphaneyi yükleyin:
   ```bash
   pip install pyqrcode
    ```
3. Kodu çalıştırın:
    ```bash
    python main.py
    ```
# FOR ENG

# 📱 Python QR Code Generator

This project is a tool that converts any user-input URL or text into a **vector-based (SVG)** QR code using Python and the `pyqrcode` library.

To explore data encoding and digital automation tools.

## 🚀 Features

- **Fast Conversion:** Converts URLs to QR codes in seconds.
- **Vector Output (.svg):** Provides scalable output without quality loss (pixelation).
- **User Friendly:** Works with simple input-output logic via the terminal.

## 🛠️ Technologies Used

- **Python 3.x**
- **[pyqrcode](https://pypi.org/project/PyQRCode/):** For the QR code encoding algorithm.

## 📦 Installation

1. Clone the repository.
2. Install the required library:
   ```bash
   pip install pyqrcode
   ```
3. Run the script:
   ```bash
   python main.py
   ```